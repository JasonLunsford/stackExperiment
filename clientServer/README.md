## Angular-Meteor Single Server PoC

Proof we can bring a minimongodb up, interact with it application wide, all without iFrames.

Must be able to:

* Work with ACEditor
* Renderer Window (tech TBD)
* Push CSS automatically (for now, pull off FS)